---
import AuthLayout from "@/layouts/AuthLayout.astro";
import { ResetPasswordRequestForm } from "@/components/auth/ResetPasswordRequestForm";
import { UpdatePasswordForm } from "@/components/auth/UpdatePasswordForm";

const searchParams = Astro.url.searchParams;
const hasRecoveryToken =
  searchParams.has("code") || searchParams.has("token") || searchParams.get("type") === "recovery";
---

<AuthLayout title="Reset hasła | Damix 10x Cards">
  {hasRecoveryToken ? <UpdatePasswordForm client:load /> : <ResetPasswordRequestForm client:load />}
</AuthLayout>
